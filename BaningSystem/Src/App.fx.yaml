App As appinfo:
    BackEnabled: =true
    OnStart: =ClearCollect(allitems,'Spinner List')
    Theme: =TeamsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

